#!/bin/bash
if [ "$1" == "" ]
then
    echo "Must provide model type!"
    exit
fi

while [ ! -e $1.t7 ]
do
    th trainbuild.lua -modeltype $1 -model $1.t7 -continue snapshot$1.t7 -snapshot snapshot$1.t7 -threads 8
    if [ $? -ne 0 ]; then
        exit
    fi
done
